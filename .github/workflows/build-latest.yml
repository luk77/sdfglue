name: Build SdfGlue (latest)

on:
  workflow_dispatch:
  #push:
  #  branches: [ "main" ]

permissions:
  # required to create and update GitHub releases (latest build)
  contents: write

jobs:
  build:
    uses: ./.github/workflows/build.yml
    with:
      upload_latest: true