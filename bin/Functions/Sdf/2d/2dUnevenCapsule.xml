<?xml version="1.0" encoding="UTF-8"?>
<FuncDef groupName="Basic 2d" displayName="Uneven Capsule" functionName="sdUnevenCapsule">
<Parameters>
  <Param type="float" displayName="Radius A" parameterName="radiusA" default="0.5" speed="0.01"/>
  <Param type="float" displayName="Radius B" parameterName="radiusB" default="0.1" speed="0.01"/>
  <Param type="float" displayName="Height"   parameterName="height"  default="1.0" speed="0.01"/>
</Parameters>
<Code>
<![CDATA[
// License: MIT
// https://www.shadertoy.com/view/4lcBWn ("height" parametrization)
float sdUnevenCapsule(in SDFG_VEC p, in float radiusA, in float radiusB, in float h)
{
    vec2 q = p.xz;

	q.x = abs(q.x);
    
    float b = (radiusA - radiusB) / h;
    vec2  c = vec2(sqrt(1.0-b*b),b);
    float k = c.x*q.y - c.y*q.x; // = cro(c,q);
    float m = dot(c,q);
    float n = dot(q,q);
    
         if( k < 0.0   ) return sqrt(n)               - radiusA;
    else if( k > c.x*h ) return sqrt(n+h*h-2.0*h*q.y) - radiusB;
                         return m                     - radiusA;
}
]]>
</Code>
</FuncDef>