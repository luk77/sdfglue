<?xml version="1.0" encoding="UTF-8"?>
<FuncDef groupName="Generalized DF" displayName="Truncated Octahedron (soft)" functionName="hgsdfTruncatedOctahedronSoft">
<Parameters>
  <Param type="float" displayName="Radius"      parameterName="radius"      default="0.5" speed="0.01"/>
  <Param type="float" displayName="Exponent"    parameterName="exponent"    default="16.0" speed="0.01"/>
</Parameters>
<Code>
<![CDATA[
// Based on: "Generalized Distance Functions" by Akleman and Chen.
// From: http://mercury.sexy/hg_sdf/
// License: MIT
//
// Code by luk77 based on: 
// https://en.wikipedia.org/wiki/Tetrakis_hexahedron
// https://en.wikipedia.org/wiki/Truncated_octahedron
// Dual polyhedron: Tetrakis Hexahedron
float hgsdfTruncatedOctahedronSoft(vec3 p, float r, float e)
{
    const float a = 1.5f;
    const float b = 1.0 / 1.5f;
    
    const vec3 v0   = vec3( 1, 1, 1);
    const vec3 v1   = vec3( 1, 1,-1);
    const vec3 v2   = vec3( 1,-1, 1);
    const vec3 v3   = vec3( 1,-1,-1);
    const vec3 v8   = vec3( a, 0, 0);
    const vec3 v10  = vec3( 0, a, 0);
    const vec3 v12  = vec3( 0, 0, a);

    float d = 0.0;
    d += pow(abs(dot(p, v0 )) * b, e);
    d += pow(abs(dot(p, v1 )) * b, e);
    d += pow(abs(dot(p, v2 )) * b, e);
    d += pow(abs(dot(p, v3 )) * b, e);
    d += pow(abs(dot(p, v8 )) * b, e);
    d += pow(abs(dot(p, v10)) * b, e);
    d += pow(abs(dot(p, v12)) * b, e);

    return pow(d, 1./e) - r;
}
]]>
</Code>
</FuncDef>