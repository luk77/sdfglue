<?xml version="1.0" encoding="UTF-8"?>
<FuncDef groupName="Basic" displayName="Box" functionName="sdBox">
<Parameters>
  <Param type="vec3" displayName="Dimensions (half)" parameterName="dim" default="0.5;0.5;0.5" speed="0.01"/>
</Parameters>
<Code>
<![CDATA[
float sdBox(in SDFG_VEC p, in vec3 dim)
{
    vec3 q = abs(p.xyz) - dim;
    return length(max(q,0.0)) + min(max(q.x,max(q.y,q.z)),0.0);
}
]]>
</Code>
</FuncDef>